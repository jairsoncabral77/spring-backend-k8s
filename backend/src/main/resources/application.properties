spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/squad
spring.datasource.username=${DB_USER:squad}
spring.datasource.password=${DB_PASSWORD:squad00}